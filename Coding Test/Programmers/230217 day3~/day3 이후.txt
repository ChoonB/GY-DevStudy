day3 이후로는 자바공부와 병행하기때문에 진척속도가 느려져 여기 한번에 모아두려한다.

Solution1 : 설명은 day2 그림에있음

32번 챌_두개뽑아서더하기 : 구상자체는 쉽게했으나 set에서 배열변환이 힘들었다.
hashset을 하나 만들고 이중포문으로 합을 집어넣어 중복값 없에고
set을 arraylist로 옮기고 오름차순정렬 뒤 그걸 다시 배열로 옮겨서 해결했다.

33번 챌_로또의최고순위와최저순위 : 아주쉽게해결
일단 rank= [6, 6, 5, 4, 3, 2, 1] 로 맞는개수에 따른 순위 두고
이중포문으로 맞는갯수 있으면 count 올리고
그 안에 0갯수 찾는거 zerocount올리는거 넣어서
두개 순위 따로 제시하면 끝.

34번 챌_모의고사 : 노가다식으로 해결 어려운문제는 아님
정답배열 만들고
c1~3, t1~3 = 0으로 만들고
정답카운팅이랑 횟수 카운팅 따로 돌게해서
횟수 다돌면 t1 = 0 이 되도록 반복문 설정
그렇게 해서 정답은 일일이 케이스 나눠서 설정.

35번 챌_문자열내마음대로정렬하기 : 쉬운문제인데 헤맸다.
일단 sort하고 index n번째 문자열 뽑아낸 뒤에 char[]로 만들고 그것도 sort
그리고 중요한건 answer에 string[]하나씩만 넣어야되고 중복 안되니까
일단 int[]하나 만들어서 0 1로 된 기록지하나 만듬 
이중포문 안에다가 answer[i] != null 이면 break; 하고
char[i]와 String[j]가 같으면서 int[]에 0인거만
answer[i] 안에 String[j] 넣으면서 int[]에 1부여해주면 됨.
중요한건 기록지 int[]만들어서 활용하는거!!!

36번 챌_문자열내림차순으로배치하기 : 쉬움
String을 char배열로 저장
ArrayList 소문자 대문자 2개 생성해서 각각에 add
Collections.sort(배열이름, Collections.reverseOrder()); 으로 각각 역정렬
String.valueOf(배열이름.get(i)) 로 char를 String으로 변환해서 answer에 저장.

37번 챌_소수만들기 : 쉬움
3중포문으로 i=0 j=i+1 k=j+1 로 설계한다음 그안에 for문 하나 더
for (int l = 1; l < (nums[i]+nums[j]+nums[k]); l++)  자기자신으로는 안나눈다
nums[i]+nums[j]+nums[k] % l == 0일때 카운트 올려서
count가 2넘어가면 바로 break, count가 1(1로만 나누어 떨어지는 수) 일때만 answer++

38번 챌_숫자문자열과영단어 : replace만 알면 쉬움
str2 = str1.replace(바꿔야하는문자열,바꿀문자열);
Integer.toString();	int > String
Integer.parseInt();	String > int

39번 챌_시저암호 : 쉽게 해결
char의 초기값은 \u0000 이다 이거만 알면 쉽게 해결가능

40번 챌_신규아이디추천 : 시간이 걸렸으나 해결
오타 조심하자..
정규식 사용하면 쉽게해결가능