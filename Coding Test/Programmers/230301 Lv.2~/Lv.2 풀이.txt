1. T_최댓값과최솟값	: 쉽게 품
s.split(" ")으로 분리해 String[]에 저장한 다음
int[] 로 바꾸고 min max 변수 만들어 저장하고 답제출

2. T_JadenCase문자열만들기 : 문제가 이상함
다른사람의 풀이가 예술이었다
별개로 문제는 공백처리가 이상해서 좀..
공백을 문자로 인정하는건 무슨 경우인지

3. T_올바른괄호 : 간단한데 설계에서 좀 어려움
스택을 써서 (면 push 
)면 stack이 비어있으면 false고 있으면 pop
결과적으로 stack이 비면 true

4. T_피보나치수 : 쉬운편
피보나치수는 배열로 저장.
근데 애초에 %1234567을 한 것을 저장시키면 에러 안뜸
arr[i] = (arr[i-1] + arr[i-2])% 1234567;

5. T_짝지어 제거하기 : 중간
만난짝끼리만 제거
스택을 써서 다르면 넣고 같으면 빼서 없에기
스택.empty()면 짝 맞은거니 끝