복습필요한문제 : 21번 24번


21번 이상한 문자 만들기   << 어려움
.split("") : ""으로 자르면 글자 하나하나가 배열로 들어감(공백포함)
String[] a = s.split(""); : 문자열 s를 글자하나하나 배열a로 저장(공백포함)

.String.join("",a) : 문자열 배열a를 ""로 join하면 붙은 문자열이 됨.
vs
String answer += a[i] : for문으로 돌려서 문자열 뒤에 하나하나 붙이는 방법
후자는 되고 전자는 안되는 이유?

22번 자릿수더하기 : 쉽게품
각 자리수를 String 배열로 저장(s = ""+n 한 뒤 str = s.split(""))해서
Integer.parseInt()을써서 문자열을 숫자로 바꾸고 누적합.

23번 자연수뒤집어배열로만들기 : 쉽게품
자연수 n 을  String배열로 저장(s = ""+n 한 뒤 str = s.split(""))해서
for (int i = str.length-1, j = 0; i >= 0 ; i--, j++)
for문에 i는 뒤에서 j는 0에서 시작하게 해서 i,j 값 뒤집어
Integer.parseInt()을써서 문자열을 숫자로 바꿔서 int배열로 저장

24번 정수내림차순으로배치하기 : 쉽게 풀었지만 런타임에러 났었다.
String[] str = s.split("");	// 문자열s를 글자마다 분해해 배열str에 넣는다
Arrays.sort(str);		// 오름차순으로 정렬한다.
String p = String.join("",rev);// 배열rev에 있는 각각의 원소를 합쳐 문자열p에 넣는다.

런타임 에러 나는 이유!!!
long answer 로 선언했는데 
Integer.parseInt(p);	로 바꿔서 런타임에러
Long.parseLong(p); 로 쓰면 런타임에러 해결

25번 정수제곱근 판별 : 쉽게품
n과 answer가 long이라서
for문으로 안돌리고 long i 로 선언 후 while로 돌리고
안에 break 넣은 if문 사용.

26번 제일 작은수 제거하기 : 바로 풀었으나 반복문 구상에 시간 좀 걸림
제일 처음에 예외처리해두고
반복문으로 최소값과 최소값의 index값 변수에 저장한 뒤
저장하는 반복문에서 변수를 2개로 int s = 0; int t = -1;두고
while 반복문으로 t++; if(t==a){continue;} answer[s] = arr[t]; s++해서
중간 continue로 최소값index만 skip시켜 처리.

27번 콜라츠추측 : 쉬우나 long int에서 약간 문제있었음
그냥 경우 나눠서 if문으로 해결하면됨.
근데 중간결과값이 int에서 오버플로우 되서 오류 뜸.
중간 결과값만 long으로 선언해주면 해결.

28번 하샤드수 : 쉽게품
입력값 String 배열로 자릿수별로 저장한뒤에
for문으로 Integer.parseInt(str[i]) 한 값 sum에 저장시키고 나머지값 확인.

29번 챌_3진법뒤집기 : 쉽게품
진법변환해서 char배열로 바꿔서 뒤집은다음에 다시 진법변환

30번 챌_최소직사각형 : 쉽게품
크기 = 전체 최대값 * 각 행에서 작은값들 중 최대값
가로세로 다 합쳐서 최대값하나 반복문 2중으로 뽑아내고
같은 행 안에서 더 작은값을 모아 반복문으로 배열 하나 만든다음에
그 배열에서 최대값 반복문으로 찾아내고
최대값 2개끼리 곱하면 끝.

31번 챌_같은숫자는싫어 : 컬렉션프레임워크 쓰니까 진짜 쉽게 품
가장 최근값 비교해서 하는거라 스택 씀.
스택하나 만들고 스택에서 peek한거랑 저장할값 비교해서 넣으면 바로 됨

solution1 : 이미지에 설명
과제2는 day3에있음